services:
  - type: web
    name: kite-trader-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 300
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
