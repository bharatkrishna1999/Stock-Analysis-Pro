services:
  - type: web
    name: stock-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn stock_trading_system:app --timeout 300
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
